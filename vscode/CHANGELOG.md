# Change Log

## 0.1.0

- Initial ParseLens preview extension release.
- Preview JSON, JSONC, JSON5, and Python `repr`-like diagnostics as canonical JSON.
- Open parsed output in a read-only JSON preview editor without modifying the source document.
- Bundle the Rust WebAssembly parser backend with the extension.
