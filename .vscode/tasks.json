{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "vscode: build extension",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/vscode"
      },
      "group": "build",
      "problemMatcher": "$tsc"
    }
  ]
}
